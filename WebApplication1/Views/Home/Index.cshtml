@{
    ViewData["Title"] = "Home Page";
}

<style>
    body {
        margin: 0;
        padding: 0;
        overflow-x: hidden;
    }

    /* Hero Slider Section */
    .hero-slider {
        position: relative;
        height: 500px;
        overflow: hidden;
        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
    }

    .slide {
        position: absolute;
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        transition: opacity 1s ease-in-out;
        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
    }

    .slide.active {
        opacity: 1;
    }

    .slide-content {
        text-align: center;
        color: white;
        padding: 2rem;
        max-width: 800px;
        animation: slideUp 0.8s ease-out;
    }

    @@keyframes slideUp {
        from {
            transform: translateY(30px);
            opacity: 0;
        }
        to {
            transform: translateY(0);
            opacity: 1;
        }
    }

    .slide-content h1 {
        font-size: 3rem;
        font-weight: 700;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    .slide-content p {
        font-size: 1.3rem;
        margin-bottom: 2rem;
        opacity: 0.95;
    }

    .slide-icon {
        font-size: 5rem;
        margin-bottom: 1.5rem;
        animation: bounce 2s infinite;
    }

    @@keyframes bounce {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-20px); }
    }

    /* Slider Navigation */
    .slider-nav {
        position: absolute;
        bottom: 30px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        gap: 12px;
        z-index: 10;
    }

    .slider-dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
        background: rgba(255,255,255,0.5);
        cursor: pointer;
        transition: all 0.3s;
        border: 2px solid transparent;
    }

    .slider-dot.active {
        background: white;
        transform: scale(1.3);
        border-color: rgba(255,255,255,0.5);
    }

    .slider-arrows {
        position: absolute;
        top: 50%;
        width: 100%;
        display: flex;
        justify-content: space-between;
        padding: 0 2rem;
        transform: translateY(-50%);
        z-index: 10;
    }

    .slider-arrow {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: rgba(255,255,255,0.2);
        border: 2px solid rgba(255,255,255,0.5);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.3s;
        font-size: 1.5rem;
    }

    .slider-arrow:hover {
        background: rgba(255,255,255,0.3);
        transform: scale(1.1);
    }

    /* Welcome Section for Non-Authenticated */
    .welcome-cta {
        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
        color: white;
        padding: 4rem 2rem;
        text-align: center;
    }

    .welcome-cta h3 {
        font-size: 2rem;
        margin-bottom: 1.5rem;
    }

    .welcome-cta .btn-primary {
        background: #f59e0b;
        border: none;
        padding: 0.8rem 2.5rem;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 8px;
        transition: all 0.3s;
    }

    .welcome-cta .btn-primary:hover {
        background: #d97706;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(245, 158, 11, 0.4);
    }

    /* Services Container */
    .services-container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 2rem;
        padding: 3rem 2rem;
        max-width: 1400px;
        margin: 0 auto;
        background: #f8fafc;
    }

    .service-card {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        text-align: center;
        box-shadow: 0 4px 6px rgba(0,0,0,0.07);
        transition: all 0.3s ease;
        border: none;
        position: relative;
        overflow: hidden;
    }

    .service-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, #1e3c72 0%, #2a5298 100%);
        transform: scaleX(0);
        transition: transform 0.3s ease;
    }

    .service-card:hover::before {
        transform: scaleX(1);
    }

    .service-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 12px 24px rgba(30, 60, 114, 0.15);
    }

    .service-icon {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem;
        font-size: 2rem;
        transition: all 0.3s ease;
    }

    .service-card:hover .service-icon {
        transform: rotate(360deg) scale(1.1);
    }

    .service-title {
        color: #1e3c72;
        font-size: 1.4rem;
        font-weight: 700;
        margin-bottom: 1rem;
    }

    .service-description {
        color: #64748b;
        font-size: 0.95rem;
        line-height: 1.6;
        margin-bottom: 1.5rem;
    }

    .service-button {
        display: inline-block;
        background: #f59e0b;
        color: white;
        padding: 0.75rem 2rem;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s;
        border: none;
    }

    .service-button:hover {
        background: #d97706;
        color: white;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(245, 158, 11, 0.4);
    }

    /* Features Section */
    .features-section {
        background: linear-gradient(to bottom, #f8fafc 0%, #ffffff 100%);
        padding: 3rem 2rem;
        text-align: center;
    }

    .features-title {
        color: #1e3c72;
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 3rem;
    }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 2rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    .feature-item {
        padding: 1.5rem;
    }

    .feature-icon {
        font-size: 2.5rem;
        color: #2a5298;
        margin-bottom: 1rem;
    }

    .feature-text {
        color: #475569;
        font-size: 0.9rem;
        font-weight: 600;
    }

    @@media (max-width: 768px) {
        .slide-content h1 {
            font-size: 2rem;
        }

        .slide-content p {
            font-size: 1rem;
        }

        .slide-icon {
            font-size: 3rem;
        }

        .hero-slider {
            height: 400px;
        }

        .services-container {
            grid-template-columns: 1fr;
            padding: 2rem 1rem;
        }

        .slider-arrows {
            padding: 0 1rem;
        }

        .slider-arrow {
            width: 40px;
            height: 40px;
            font-size: 1.2rem;
        }
    }
</style>

@if (User?.Identity?.IsAuthenticated == true)
{
    <!-- Hero Slider -->
    <div class="hero-slider">
        <div class="slide active" data-slide="0">
            <div class="slide-content">
                <div class="slide-icon">
                    <i class="fas fa-graduation-cap"></i>
                </div>
                <h1>Welcome to UC Student Portal</h1>
                <p>Your gateway to student services, reservations, and locker management</p>
            </div>
        </div>

        <div class="slide" data-slide="1">
            <div class="slide-content">
                <div class="slide-icon">
                    <i class="fas fa-calendar-check"></i>
                </div>
                <h1>Easy Reservations</h1>
                <p>Book study rooms and schedule appointments with just a few clicks</p>
            </div>
        </div>

        <div class="slide" data-slide="2">
            <div class="slide-content">
                <div class="slide-icon">
                    <i class="fas fa-lock-open"></i>
                </div>
                <h1>Secure Locker Management</h1>
                <p>Select, manage, and track your locker assignments effortlessly</p>
            </div>
        </div>

        <div class="slide" data-slide="3">
            <div class="slide-content">
                <div class="slide-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <h1>Gate Pass System</h1>
                <p>Request and manage your campus access passes seamlessly</p>
            </div>
        </div>

        <!-- Slider Navigation -->
        <div class="slider-arrows">
            <div class="slider-arrow" id="prevSlide">
                <i class="fas fa-chevron-left"></i>
            </div>
            <div class="slider-arrow" id="nextSlide">
                <i class="fas fa-chevron-right"></i>
            </div>
        </div>

        <div class="slider-nav">
            <span class="slider-dot active" data-slide="0"></span>
            <span class="slider-dot" data-slide="1"></span>
            <span class="slider-dot" data-slide="2"></span>
            <span class="slider-dot" data-slide="3"></span>
        </div>
    </div>
}
else
{
    <div class="welcome-cta">
        <h3>Please log in to access the UC Student Portal</h3>
        <a class="btn btn-primary mt-3" asp-controller="Account" asp-action="Login">Log in</a>
    </div>
}

<!-- Services Cards -->
<div class="services-container">
    <div class="service-card">
        <div class="service-icon">
            <i class="fas fa-user"></i>
        </div>
        <h2 class="service-title">Student Information</h2>
        <p class="service-description">Access and manage your student profile, academic records, and enrollment information.</p>
        <a asp-controller="Student" asp-action="Index" class="service-button">Access Portal</a>
    </div>

    <div class="service-card">
        <div class="service-icon">
            <i class="fas fa-calendar"></i>
        </div>
        <h2 class="service-title">Reservation System</h2>
        <p class="service-description">Book study rooms, schedule faculty appointments, and manage campus reservations.</p>
        <a asp-controller="Reservation" asp-action="Add" class="service-button">Make Reservation</a>
    </div>

    <div class="service-card">
        <div class="service-icon">
            <i class="fas fa-lock"></i>
        </div>
        <h2 class="service-title">Locker Selection</h2>
        <p class="service-description">Select and manage your locker, view availability, and request locker changes.</p>
        <a asp-controller="LockerRequests" asp-action="Create" class="service-button">Select Locker</a>
    </div>

    <div class="service-card">
        <div class="service-icon">
            <i class="fas fa-shield-alt"></i>
        </div>
        <h2 class="service-title">Gate Pass Management</h2>
        <p class="service-description">Monitor and request access for campus entry or exit. Submit, track, and review your approved or pending gate pass requests.</p>
        <a asp-controller="GatePass" asp-action="Index" class="service-button">Request Gate Pass</a>
    </div>
</div>

<!-- Features Section -->
<div class="features-section">
    <h2 class="features-title">Why Choose UC Student Portal?</h2>
    <div class="features-grid">
        <div class="feature-item">
            <div class="feature-icon">
                <i class="fas fa-bolt"></i>
            </div>
            <p class="feature-text">Fast & Efficient</p>
        </div>
        <div class="feature-item">
            <div class="feature-icon">
                <i class="fas fa-clock"></i>
            </div>
            <p class="feature-text">24/7 Access</p>
        </div>
        <div class="feature-item">
            <div class="feature-icon">
                <i class="fas fa-user-shield"></i>
            </div>
            <p class="feature-text">Secure & Private</p>
        </div>
    </div>
</div>

<script>
    // Hero Slider Functionality
    let currentSlide = 0;
    const slides = document.querySelectorAll('.slide');
    const dots = document.querySelectorAll('.slider-dot');
    const totalSlides = slides.length;

    function showSlide(index) {
        // Remove active class from all slides and dots
        slides.forEach(slide => slide.classList.remove('active'));
        dots.forEach(dot => dot.classList.remove('active'));

        // Add active class to current slide and dot
        slides[index].classList.add('active');
        dots[index].classList.add('active');
    }

    function nextSlide() {
        currentSlide = (currentSlide + 1) % totalSlides;
        showSlide(currentSlide);
    }

    function prevSlide() {
        currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
        showSlide(currentSlide);
    }

    // Auto slide every 5 seconds
    let autoSlide = setInterval(nextSlide, 5000);

    // Arrow click handlers
    document.getElementById('nextSlide')?.addEventListener('click', () => {
        clearInterval(autoSlide);
        nextSlide();
        autoSlide = setInterval(nextSlide, 5000);
    });

    document.getElementById('prevSlide')?.addEventListener('click', () => {
        clearInterval(autoSlide);
        prevSlide();
        autoSlide = setInterval(nextSlide, 5000);
    });

    // Dot click handlers
    dots.forEach(dot => {
        dot.addEventListener('click', () => {
            clearInterval(autoSlide);
            currentSlide = parseInt(dot.getAttribute('data-slide'));
            showSlide(currentSlide);
            autoSlide = setInterval(nextSlide, 5000);
        });
    });

    // Pause on hover
    document.querySelector('.hero-slider')?.addEventListener('mouseenter', () => {
        clearInterval(autoSlide);
    });

    document.querySelector('.hero-slider')?.addEventListener('mouseleave', () => {
        autoSlide = setInterval(nextSlide, 5000);
    });
</script>
